import { describe, test, expect } from "bun:test";
import { sort } from "../src/index";

describe("sort", () => {
    test.todo("should correctly quicksort");
    test.todo("should correctly argsort");
    test.todo("should correctly check isSorted");
    test.todo("should correctly selectKth");
});
