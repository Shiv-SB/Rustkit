import { describe, test, expect } from "bun:test";
import { geohash } from "../src/index";

describe("geohash", () => {
    test.todo("should correctly encode");
    test.todo("should correctly decode");
    test.todo("should correctly find neighbor");
    test.todo("should correctly compute distance");
});
