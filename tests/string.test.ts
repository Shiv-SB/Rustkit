import { describe, test, expect } from "bun:test";
import { string } from "../src/index";

describe("string", () => {
    test.todo("should correctly compute levenshtein distance");
    test.todo("should correctly compute hamming distance");
    test.todo("should correctly fuzzy match");
    test.todo("should correctly compute longest common subsequence");
    test.todo("should correctly compute longest common substring");
});
