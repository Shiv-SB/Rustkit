import { describe, test, expect } from "bun:test";
import { quantile } from "../src/index";

describe("quantile", () => {
    test.todo("should correctly add to t-digest");
    test.todo("should correctly query t-digest quantile");
    test.todo("should correctly add to count-min sketch");
    test.todo("should correctly insert into bloom filter");
});
