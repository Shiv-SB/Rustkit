/// Byte-for-byte exact-match comparison of two sequences. Returns `true` only
/// if they are identical.
///
/// TODO(eval): implement. Stub currently returns `false`.
pub fn exact_match(_reference: &[u8], _hypothesis: &[u8]) -> bool {
    false
}